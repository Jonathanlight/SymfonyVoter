{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="section">

            <!--   Icon Section   -->
            {{ dump(path('register')|slice(0,9)) }}
            <div class="row">
                {{ form(form, {attr: {action: path('register')|slice(0,9) }}) }}

            </div>

        </div>
        <br><br>
    </div>
{% endblock %}
